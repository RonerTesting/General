//Modules
let {roner} = require('./4-names')
let sayHi = require('./5-utils')
let data = require('./6-alternative-flavor')
require('./7-mind-grenade')
// console.log(data)
//CommonJS, every file is a module (by default)
// Module  - Encapsulated Code (only share  minimum)


// sayHi(roner);